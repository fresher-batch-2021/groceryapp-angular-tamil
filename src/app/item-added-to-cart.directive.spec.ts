import { ItemAddedToCartDirective } from './item-added-to-cart.directive';

describe('ItemAddedToCartDirective', () => {
  it('should create an instance', () => {
    const directive = new ItemAddedToCartDirective();
    expect(directive).toBeTruthy();
  });
});
